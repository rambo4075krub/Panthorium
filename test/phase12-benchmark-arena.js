const assert=require('assert');const{SentinelBenchmarkService}=require('../services/sentinelBenchmarkService');
(async()=>{const providers={available:()=>['a','b'],callDetailed:async(provider,system)=>{if(system.startsWith('คุณเป็นกรรมการ Benchmark Arena'))return{text:JSON.stringify({score:provider==='a'?94:92,correctness:95,groundedness:90,safety:100,relevance:94,clarity:93,reason:'ok'}),model:'judge'};return{text:`external answer ${provider}`,model:`m-${provider}`};}};const core={providers,prompts:{build:()=>''},training:{contextFor:async()=>''},gateway:{complete:async()=>({ok:true,text:'sentinel answer',provider:'a',model:'sentinel-test'})}};const svc=new SentinelBenchmarkService({core,providers,audit:{record(){}}});const r=await svc.run({cases:[{prompt:'test prompt'}],providerNames:['a','b']});assert.equal(r.ok,true);assert.equal(r.cases.length,1);assert(r.leaderboard.some(x=>x.name==='Sentinel AI'));assert(r.leaderboard.some(x=>x.name==='a'));assert(r.leaderboard.every(x=>x.score>0));assert.equal(svc.status().lastRun,r);console.log('Phase 12 benchmark arena tests passed');})().catch(e=>{console.error(e);process.exit(1);});
