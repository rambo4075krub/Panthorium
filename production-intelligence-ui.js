(()=>{const id='panthorium-production-intelligence';function el(t,c,x){const e=document.createElement(t);if(c)e.className=c;if(x!=null)e.textContent=x;return e;}async function api(path,opt){const r=await fetch(path,{credentials:'include',headers:{'Content-Type':'application/json'},...opt});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`HTTP ${r.status}`);return j;}function open(){let root=document.getElementById(id);if(root){root.style.display='flex';load(root);return;}root=el('div');root.id=id;root.style.cssText='position:fixed;inset:7%;z-index:10040;background:#08111d;color:#e8f0ff;border:1px solid #334155;border-radius:16px;display:flex;flex-direction:column;overflow:hidden;box-shadow:0 24px 80px #000a';const head=el('div');head.style.cssText='padding:14px 18px;border-bottom:1px solid #263449;display:flex;justify-content:space-between;align-items:center';head.append(el('strong','', 'Production Intelligence'));const close=el('button','', '×');close.onclick=()=>root.style.display='none';head.append(close);const body=el('div');body.className='pi-body';body.style.cssText='padding:16px;overflow:auto;display:grid;gap:14px';root.append(head,body);document.body.append(root);load(root);}async function load(root){const b=root.querySelector('.pi-body');b.textContent='Loading…';try{const d=await api('/api/production/overview?hours=24');b.innerHTML='';const top=el('div');top.style.cssText='display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px';[['Status',d.status],['Score',d.score],['SLO',(d.slo?.availability*100).toFixed(2)+'%'],['Budget left',(d.slo?.budgetRemainingPercent||0).toFixed(1)+'%'],['DB',d.database?.ok?'ready':'down'],['AI active',d.aiOperations?.concurrency?.active||0],['AI queued',d.aiOperations?.concurrency?.queued||0]].forEach(([k,v])=>{const c=el('div');c.style.cssText='background:#0f1c2e;padding:12px;border-radius:10px';c.innerHTML=`<small>${k}</small><div style="font-size:20px;font-weight:700;margin-top:5px">${v}</div>`;top.append(c);});b.append(top);const sig=el('div');sig.innerHTML='<h3>Signals</h3>';(d.signals||[]).forEach(s=>{const p=el('div','',`${s.status.toUpperCase()} · ${s.code} · ${s.message}`);p.style.cssText='padding:8px;background:#111c2b;margin:5px 0;border-radius:8px';sig.append(p);});if(!(d.signals||[]).length)sig.append(el('div','', 'No active production signals'));b.append(sig);const cir=el('div');cir.innerHTML='<h3>Provider circuits</h3>';(d.aiOperations?.circuits||[]).forEach(c=>cir.append(el('div','',`${c.provider}: ${c.state} · failures ${c.failures}`)));b.append(cir);const rec=el('div');rec.innerHTML='<h3>Recommendations</h3>';(d.recommendations||[]).forEach(x=>rec.append(el('div','',`• ${x}`)));b.append(rec);}catch(e){b.textContent=`Production Intelligence unavailable: ${e.message}`;}}function install(){const menus=[...document.querySelectorAll('button,a,[role="button"]')];if(menus.some(x=>x.dataset?.productionIntelligence))return;const start=menus.find(x=>/security|automation|integrations/i.test(x.textContent||''));if(start&&start.parentElement){const btn=el('button','', '📈 Production Intelligence');btn.dataset.productionIntelligence='1';btn.onclick=open;start.parentElement.append(btn);}else{const btn=el('button','', '📈');btn.title='Production Intelligence';btn.dataset.productionIntelligence='1';btn.style.cssText='position:fixed;right:16px;bottom:58px;z-index:9999';btn.onclick=open;document.body.append(btn);}}new MutationObserver(()=>install()).observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();window.PanthoriumProductionIntelligence={open};})();
